# Project

## Purpose

Describe what this repository does and the user-visible outcome it exists to provide.

## Architecture

Record only stable architecture facts that materially reduce rediscovery for future workers.

## Important boundaries

- identify high-risk areas;
- identify generated or vendored paths that should not be edited directly;
- identify external systems only at the level needed to work safely;
- never store secrets here.

## Verification

The canonical local verification entry point is `dev/verify-local.sh`.

Update this document only when durable project knowledge changes. Temporary task status belongs in the work item, not here.
