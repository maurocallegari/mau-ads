# Repository map

Use this file only when the repository structure is not obvious.

| Path | Purpose |
|---|---|
| `src/` | primary implementation |
| `tests/` | automated checks |
| `dev/` | local development and verification helpers |
| `.ai/` | machine-readable AI development metadata |

Keep this map compact. It is navigation, not duplicate architecture documentation.
